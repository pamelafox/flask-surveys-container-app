[tool.ruff]
select = ["E", "F", "I", "UP"]
target-version = "py311"
line-length = 120
src = ["src"]
extend-exclude = ["src/backend/migrations"]

[tool.black]
target-version =  ["py311"]
line-length = 120
extend-exclude = '''
(
  ^/src/backend/migrations\/
)
'''
force-exclude = '''
# Needed for pre-commit
(
  ^/src/backend/migrations\/
)
'''

[tool.pytest.ini_options]
addopts = "-ra --cov=src/backend"
testpaths = [
  "src/backend/tests"
]
pythonpath = [
  "src"
]
env_files = ['.env']

[tool.coverage.run]
omit = ["src/backend/tests/*", "src/backend/settings/production.py"]

[tool.coverage.report]
show_missing = true
fail_under = 100
