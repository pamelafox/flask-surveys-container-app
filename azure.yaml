# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: flask-surveys-container-app
metadata:
  template: flask-surveys-container-app@0.0.1-beta
services:
  web:
    project: ./src
    module: web
    language: py
    host: containerapp
